.386
.MODEL SMALL

PRINTDSEG SEGMENT PARA PUBLIC 'DATA' USE16
    TAB     DB '0123456789ABCDEF'
PRINTDSEG ENDS

PRINTCSEG SEGMENT PARA PUBLIC 'CODE' USE16
    ASSUME CS:PRINTCSEG

    PRINTSTR PROC FAR

        MOV     AH, 0Eh
    @@: LODSB 
        CMP     AL, 0
        JE      @f
        INT     10h
        JMP     @b

    @@: RET

    PRINTSTR ENDP

PRINTCSEG ENDS

END