.386
.MODEL SMALL

INCLUDE INIT.INC
INCLUDE PRINT.INC

MAINDSEG SEGMENT PARA PUBLIC 'DATA' USE16
	
MAINDSEG ENDS

MAINCSEG SEGMENT PARA PUBLIC 'CODE' USE16
	ASSUME CS:MAINCSEG, DS:INITDSEG
	
	MAIN PROC FAR

		CALL	RETURN

		MOV		SI, OFFSET MAINMSG
		CALL 	PRINTSTR
		
		MOV		AX, 4C00h
		INT		21h

	MAIN ENDP
	
MAINCSEG ENDS

END