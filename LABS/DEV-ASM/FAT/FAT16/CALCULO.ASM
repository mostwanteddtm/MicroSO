ORG 100H

;MOV AH, 08H
;INT 13H

;FORMULA ((S*H)*C)-S
;CHS=20/16/63 | ((63*16)*20)-63

MOV AX, RETORNO 
MOV DX, 0F01H
INC DH
MOV BYTE PTR HEAD, DH

AND AX, 1111111111000000B 
RCL AX, 9 
INC AX
MOV WORD PTR CYLINDER, AX

MOV AX, RETORNO
AND AX, 0000000000111111B 
MOV BYTE PTR SECTOR, AL 

XOR BX, BX
MOV BL, HEAD
MUL BX

MOV BX, CYLINDER
MUL BX

XOR BX, BX
MOV BL, SECTOR
SUB AX, BX

INT 20H  

RETORNO DW 133FH 
CYLINDER DW 0
HEAD DB 0 
SECTOR DB 0